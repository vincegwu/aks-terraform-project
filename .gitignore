# Terraform state and lock files
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Terraform provider cache (large binaries)
.terraform/

# Environment-specific variables (contain secrets)
envs/

# Kubeconfig files
.kube/
kubeconfig*

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Terraform logs
crash.log
crash.*.log

# Terraform override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI files
.terraformrc
terraform.rc

# Local development
*.local
.env
.env.local
.env.*.local

# Terraform state files (now in remote backend)
*.tfstate
*.tfstate.*
terraform.tfstate.d/

# Secrets and credentials
*secret*
*password*
*credentials*
*.pem
*.key
*.crt
*.pfx
*.p12

# Application code (fetched during deployment)
app-code/

# Terraform outputs with sensitive data
terraform-outputs.json

# Kubernetes secrets (should be created via kubectl)
k8s/configmaps/*secrets*.yaml
!k8s/configmaps/app-secrets-template.yaml

# Backend config with sensitive data
backend-configs/*.tfbackend
!backend-configs/.gitkeep

# Environment variables
.env
*.env
