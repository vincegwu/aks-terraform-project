apiVersion: v1
kind: Service
metadata:
  name: book-review-backend
  namespace: book-review-dev
  labels:
    app: book-review
    component: backend
spec:
  type: ClusterIP
  selector:
    app: book-review
    component: backend
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
