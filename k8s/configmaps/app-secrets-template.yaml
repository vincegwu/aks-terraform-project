# Kubernetes Secret for Book Review Application - TEMPLATE ONLY
# 
# ⚠️  WARNING: This is a TEMPLATE file. DO NOT add real credentials here!
# ⚠️  This file should NEVER contain actual passwords or secrets.
#
# SECURITY BEST PRACTICE: Create secrets using kubectl command:
#
# kubectl create secret generic book-review-secrets \
#   --from-literal=database_user=adminuser \
#   --from-literal=database_password=<YOUR_MYSQL_PASSWORD>
#
# Or retrieve from Azure Key Vault:
#
# MYSQL_PASSWORD=$(az keyvault secret show \
#   --vault-name <keyvault-name> \
#   --name mysql-admin-password \
#   --query value -o tsv)
#
# kubectl create secret generic book-review-secrets \
#   --from-literal=database_user=adminuser \
#   --from-literal=database_password=$MYSQL_PASSWORD
#
# NOTE: In CI/CD, secrets are automatically created from GitHub Secrets

apiVersion: v1
kind: Secret
metadata:
  name: book-review-secrets
  labels:
    app: book-review
type: Opaque
stringData:
  # ⚠️  TEMPLATE ONLY - NO REAL VALUES
  # Actual secrets are created via kubectl or CI/CD pipeline
  database_user: "PLACEHOLDER_DO_NOT_USE"
  database_password: "PLACEHOLDER_DO_NOT_USE"
