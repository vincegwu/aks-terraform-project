# ⚠️ TEMPLATE ONLY — DO NOT APPLY
# Secrets are created via CI/CD or kubectl
# This file exists for documentation and structure only

apiVersion: v1
kind: Secret
metadata:
  name: book-review-secrets
  namespace: book-review-dev
  labels:
    app: book-review
type: Opaque
stringData:
  database_user: "PLACEHOLDER_DO_NOT_USE"
  database_password: "PLACEHOLDER_DO_NOT_USE"
  jwt_secret: "PLACEHOLDER_DO_NOT_USE"
