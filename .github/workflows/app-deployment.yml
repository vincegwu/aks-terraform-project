name: Application Deployment

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment Environment'
        required: true
        type: choice
        options:
          - dev
          - stage
          - prod
      frontend_image_tag:
        description: 'Frontend Docker image tag'
        required: false
        default: 'latest'
      backend_image_tag:
        description: 'Backend Docker image tag'
        required: false
        default: 'latest'
      app_repository:
        description: 'Remote GitHub repo for app (owner/repo)'
        required: false
        default: 'pravinmishraaws/book-review-app'
      app_branch:
        description: 'Branch of the app repository'
        required: false
        default: 'main'
      run_id:
        description: 'Infra workflow run ID (for fetching Terraform state)'
        required: false

env:
  K8S_PATH: './k8s'

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    outputs:
      acr-login-server: ${{ steps.get-infra.outputs.acr-login-server }}
      backend-image: ${{ steps.build-backend.outputs.backend-image }}
      frontend-image: ${{ steps.build-frontend.outputs.frontend-image }}
      aks-cluster-name: ${{ steps.get-infra.outputs.aks-cluster-name }}
      resource-group: ${{ steps.get-infra.outputs.resource-group }}
      mysql-fqdn: ${{ steps.get-infra.outputs.mysql-fqdn }}
    
    steps:
      - name: Checkout workflow repo
        uses: actions/checkout@v4

      - name: Download Terraform State
        uses: actions/download-artifact@v4
        with:
          name: terraform-state-${{ github.event.inputs.environment }}
          run-id: ${{ github.event.inputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}

      - name: Extract Terraform outputs
        id: get-infra
        run: |
          ACR_NAME=$(jq -r '.acr_name.value' terraform-outputs.json)
          ACR_LOGIN_SERVER=$(jq -r '.acr_login_server.value' terraform-outputs.json)
          AKS_CLUSTER_NAME=$(jq -r '.aks_cluster_name.value' terraform-outputs.json)
          RESOURCE_GROUP=$(jq -r '.resource_group_name.value' terraform-outputs.json)
          MYSQL_FQDN=$(jq -r '.mysql_fqdn.value' terraform-outputs.json)

          echo "acr-login-server=$ACR_LOGIN_SERVER" >> $GITHUB_OUTPUT
          echo "aks-cluster-name=$AKS_CLUSTER_NAME" >> $GITHUB_OUTPUT
          echo "resource-group=$RESOURCE_GROUP" >> $GITHUB_OUTPUT
          echo "mysql-fqdn=$MYSQL_FQDN" >> $GITHUB_OUTPUT
          echo "acr-name=$ACR_NAME" >> $GITHUB_OUTPUT

      - name: Azure Login with Service Principal
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Clone Remote Application Repo
        run: |
          rm -rf ./app
          git clone --branch ${{ github.event.inputs.app_branch }} --depth 1 \
            https://github.com/${{ github.event.inputs.app_repository }}.git ./app

      - name: Login to ACR
        run: az acr login --name ${{ steps.get-infra.outputs.acr-name }}

      - name: Build & Push Backend Image
        id: build-backend
        run: |
          BACKEND_IMAGE="${{ steps.get-infra.outputs.acr-login-server }}/book-review-backend:${{ github.event.inputs.backend_image_tag }}"
          echo "Building backend image: $BACKEND_IMAGE"
          docker build -t $BACKEND_IMAGE -f ./docker/backend.Dockerfile ./app
          docker push $BACKEND_IMAGE
          echo "backend-image=$BACKEND_IMAGE" >> $GITHUB_OUTPUT

      - name: Build & Push Frontend Image
        id: build-frontend
        run: |
          FRONTEND_IMAGE="${{ steps.get-infra.outputs.acr-login-server }}/book-review-frontend:${{ github.event.inputs.frontend_image_tag }}"
          echo "Building frontend image: $FRONTEND_IMAGE"
          docker build -t $FRONTEND_IMAGE -f ./docker/frontend.Dockerfile ./app
          docker push $FRONTEND_IMAGE
          echo "frontend-image=$FRONTEND_IMAGE" >> $GITHUB_OUTPUT

  deploy-to-aks:
    name: Deploy to AKS
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Get AKS Credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ needs.build-and-push.outputs.resource-group }} \
            --name ${{ needs.build-and-push.outputs.aks-cluster-name }} \
            --admin \
            --overwrite-existing

      - name: Update Kubernetes Manifests
        run: |
          BACKEND_IMAGE="${{ needs.build-and-push.outputs.backend-image }}"
          FRONTEND_IMAGE="${{ needs.build-and-push.outputs.frontend-image }}"
          MYSQL_FQDN="${{ needs.build-and-push.outputs.mysql-fqdn }}"

          sed -i "s|<ACR_LOGIN_SERVER>/book-review-backend:latest|$BACKEND_IMAGE|g" ${{ env.K8S_PATH }}/deployments/backend-deployment.yaml
          sed -i "s|<ACR_LOGIN_SERVER>/book-review-frontend:latest|$FRONTEND_IMAGE|g" ${{ env.K8S_PATH }}/deployments/frontend-deployment.yaml
          sed -i "s|database_host: .*|database_host: \"$MYSQL_FQDN\"|g" ${{ env.K8S_PATH }}/configmaps/app-config.yaml

      - name: Create Namespace
        run: |
          kubectl create namespace book-review-${{ github.event.inputs.environment }} --dry-run=client -o yaml | kubectl apply -f -
          kubectl config set-context --current --namespace=book-review-${{ github.event.inputs.environment }}

      - name: Create Secrets
        run: |
          kubectl create secret generic book-review-secrets \
            --from-literal=database_user=${{ secrets.MYSQL_ADMIN_USERNAME }} \
            --from-literal=database_password=${{ secrets.MYSQL_ADMIN_PASSWORD }} \
            --from-literal=jwt_secret=${{ secrets.JWT_SECRET }} \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy ConfigMap
        run: kubectl apply -f ${{ env.K8S_PATH }}/configmaps/app-config.yaml

      - name: Deploy Backend
        run: |
           kubectl apply -n book-review-${{ github.event.inputs.environment }} \
           -f ${{ env.K8S_PATH }}/deployments/backend-deployment.yaml \
           -f ${{ env.K8S_PATH }}/services/backend-service.yaml

           # Force restart to pull latest image (in case tag hasn't changed)
           echo "🔄 Forcing rollout restart to ensure latest image is used..." 
           kubectl rollout restart deployment/book-review-backend -n book-review-${{ github.event.inputs.environment }}

           
           echo "⏳ Waiting for backend deployment to roll out..."
           kubectl rollout status deployment/book-review-backend -n book-review-${{ github.event.inputs.environment }} --timeout=300s || echo "⚠️ Rollout timeout - check debug output"
          
           echo ""
           echo "📊 Current backend pod status:"
           kubectl get pods -l component=backend -n book-review-${{ github.event.inputs.environment }}
           echo "✅ Backend deployment applied"

      - name: Deploy Frontend & Service
        run: |
          kubectl apply -f ${{ env.K8S_PATH }}/deployments/frontend-deployment.yaml
          kubectl apply -f ${{ env.K8S_PATH }}/services/frontend-service.yaml
          kubectl rollout status deployment/book-review-frontend --timeout=300s

      - name: Deploy Ingress
        run: |
          kubectl apply -f ${{ env.K8S_PATH }}/ingress.yaml
